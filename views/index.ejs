<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head') %> 
    <body>
        <%- include('./partials/nav') %> 
        <main>
            <% if (messages.length === 0) { %> 
                <h3>No messages to display</h3>
            <% } else { %> 
                <% messages.forEach((message) => { %>

                    <article>
                        <div class="message-header">
                            <h3>From: <%= message.author %></h3>
                            <p>Added: <%= message.time %></p>
                        </div>
                        <hr />
                        <p><%= message.text %></p>
                    </article>

                <% }) %>
            <% } %>
        </main>
    </body>
</html>